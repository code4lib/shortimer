<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

    <title>code4lib jobs</title>
    <link rel="self" type="application/atom+xml" href="{% url feed %}" />
    {% if page.has_next %}<link rel="next" type="application/atom+xml" href="{% url feed_page page.next_page_number %}" />{% endif %}
    {% if page.has_previous %}<link rel="previous" type="application/atom+xml" href="{% url feed_page page.previous_page_number %}" />{% endif %}
    <author>
        <name>code4lib</name>
        <uri>http://code4lib.org</uri>
    </author>
    <updated>{{ feed_updated }}</updated>

    {% for job in page.object_list %}
    <entry>
        <id>http://jobs.code4lib.org{% url job job.id %}</id>
        <title>{{ job.title }}</title>
        <link rel="alternate" type="text/html" href="{% url job job.id %}" />
        {% if job.contact_name and job.contact_email %}
        <author>
        <name>{{ job.contact_name }}</name>
        <email>{{ job.contact_email }}</email>
        </author>
        {% endif %}
        {% if job.creator %}
        <author>
            <name>{{ job.creator.get_full_name }}</name>
            <uri>http://jobs.code4lib.org{% url user job.creator.username %}</uri>
        </author>
        {% endif %}
        <updated>{{ job.updated }}</updated>
        <content type="html">
            {{ job.description }}
        </content>
    </entry>
    {% endfor %}

</feed>

